"use strict";
const mongoose = require("mongoose");
const Schema = mongoose.Schema;


const codigoSchema = new Schema({
    codigo: String,


  });

  export default models.codigoModel || model("codigo", codigoSchema )